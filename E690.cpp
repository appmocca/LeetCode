/*
// Definition for Employee.
class Employee {
public:
    int id;
    int importance;
    vector<int> subordinates;
};
*/

class Solution {
public:
    int getImportance(vector<Employee*> employees, int id) {
        int sum = 0 ;
        for ()
        vector<int> record ;
        int len = employees[3].size() ;
        for (int i = 0 ; i < len ; i++) record.push_back(employees[3][i]) ;

    }
};